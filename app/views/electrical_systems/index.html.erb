<div class="tab-content default-tab" id="tab1"> <!-- This is the target div. id must match the href of this div's tab -->
  <table>
    <thead>
      <tr>
        <th><input class="check-all" type="checkbox" /></th>
        <th><%= t'.id' %></th>
        <th><%= t'.driver_id' %></th>
        <th><%= t'.truck_id' %></th>  
        <th><%= t'.created_at' %></th>
      </tr>

    </thead>

    <tfoot>
      <tr>
        <td colspan="6">
          <div class="bulk-actions align-left">
            <%= render "layouts/select_action" %>
          </div>
          <%= render "layouts/pagination" %>
          <div class="clear"></div>
        </td>
      </tr>
    </tfoot>

    <tbody>
      <% @electrical_systems.each do |electrical_system| %>
        <tr>
          <td><input type="checkbox" /></td>
          <%if can? :show, ElectricalSystem %>
            <td><%=link_to electrical_system.id, electrical_system, :class => 'button'%></td>
          <%else%>
            <td><%= label electrical_system.id, electrical_system.id, :class => 'button'%></td>
          <%end%>


          <% @model = electrical_system %> 

          <td><%= electrical_system.truck.modelbrand %></td>

          <td><%= electrical_system.driver.user.fullname %></td>

          <td><%= I18n.l electrical_system.created_at, :format => :long %></td>

          <%= render "layouts/index_links" %>
        <% end %>
      </tr>
    </tbody>
  </table>
  <%= render "layouts/action_links" %>
</div> 
<%= render "layouts/second_tab" %>
<br><br><br>